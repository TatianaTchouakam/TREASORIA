# Minimal forecast dependencies; the repository-wide environment is left intact.
# Exact versions used for each saved run are recorded in run_metadata.json.
numpy>=1.26,<3
pandas>=2.2,<3
prophet>=1.1.6,<2
statsmodels>=0.14.4,<0.16
matplotlib>=3.8,<4
nbformat>=5.10,<6
nbclient>=0.10,<1
ipykernel>=6,<8
